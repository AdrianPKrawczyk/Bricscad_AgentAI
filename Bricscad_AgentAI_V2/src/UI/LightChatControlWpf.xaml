<UserControl x:Class="Bricscad_AgentAI_V2.UI.LightChatControlWpf"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="350"
             Background="#1E1E24"
             KeyDown="UserControl_KeyDown"
             FontFamily="Segoe UI, Calibri, sans-serif">
    <UserControl.Resources>
        <!-- Style for the scrollbar -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb Background="#444455" Margin="1" />
                                </Track.Thumb>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <!-- Header (HUD) -->
            <RowDefinition Height="Auto"/>
            <!-- Messages Area -->
            <RowDefinition Height="*"/>
            <!-- Input Area -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Modern HUD -->
        <Border Grid.Row="0" Background="#2B2D35" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="5" Opacity="0.2" Direction="-90" ShadowDepth="2" />
            </Border.Effect>
            <StackPanel Orientation="Vertical">
                <TextBlock x:Name="lblStatus" Text="[Model] Inicjalizacja..." Foreground="#00E5FF" FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock x:Name="lblSession" Text="Sesja: Brak" Foreground="#F2C94C" FontSize="11" Margin="0,0,10,0"/>
                    <TextBlock x:Name="lblStats" Text="Tokeny: 0 | Czas: 0s" Foreground="#828282" FontSize="11"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Chat History -->
        <Border Grid.Row="1" Background="#2B2D35" CornerRadius="8" Padding="2" Margin="0,0,0,10">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="5" Opacity="0.1" Direction="-90" ShadowDepth="1" />
            </Border.Effect>
            <ScrollViewer x:Name="scrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="2">
                <RichTextBox x:Name="rtbHistory" Background="Transparent" Foreground="#E0E0E0" BorderThickness="0" IsReadOnly="True" Padding="10">
                    <FlowDocument LineHeight="1.4">
                        <!-- Content -->
                    </FlowDocument>
                </RichTextBox>
            </ScrollViewer>
        </Border>

        <!-- Modern Input -->
        <Border Grid.Row="2" Background="#2B2D35" CornerRadius="15" Padding="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="txtInput" Grid.Column="0" TextWrapping="Wrap" AcceptsReturn="True" 
                         MinHeight="35" MaxHeight="120" Background="Transparent" Foreground="White" BorderThickness="0" 
                         Padding="10,8" VerticalScrollBarVisibility="Auto" FontSize="13"
                         PreviewKeyDown="txtInput_PreviewKeyDown" />
                <Button x:Name="btnSend" Grid.Column="1" Margin="5,0,2,0" Width="35" Height="35" 
                        Background="#007ACC" Foreground="White" BorderThickness="0" Cursor="Hand" Click="btnSend_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="17.5" BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#0098FF"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#005A9E"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="➤" FontSize="16" Margin="2,0,0,0"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
